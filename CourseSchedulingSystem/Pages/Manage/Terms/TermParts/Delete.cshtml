@page "{termId:Guid}/{id:Guid}"
@model CourseSchedulingSystem.Pages.Manage.Terms.TermParts.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Part of Term</h4>
    <hr />
    @if (Model.InUse)
    {
        <div class="alert alert-primary">
            This part of term can not be deleted. It is currently in use by the listed course sections.
        </div>
    }
<dl class="row">
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.TermPart.Name)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.TermPart.Name)
    </dd>
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.TermPart.StartDate)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.TermPart.StartDate)
    </dd>
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.TermPart.EndDate)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.TermPart.EndDate)
    </dd>
    @*         <dt class="col-sm-12">Course Sections</dt> *@
    @*         <dd class="col-sm-12"> *@
    @*             <ul> *@
    @*                 @foreach (var section in @Model.TermPart.CourseSections) *@
    @*                 { *@
    @*                     <li><a asp-page="../../CourseSections/Details" asp-route-id="@section.Id">@section.Identifier - @section.Title</a></li> *@
    @*                 } *@
    @*             </ul> *@
    @*         </dd> *@
</dl>

    <form method="post">
        <input type="hidden" asp-for="TermPart.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" disabled="@(Model.InUse ? "" : null)"/> |
        <a asp-page="../Edit" asp-route-id="@Model.TermPart.TermId">Back to List</a>
    </form>
</div>